# database-schema
A simple SQL schema for a task management system. This repository contains the database structure for managing users and their tasks, including example records. The schema is written in PostgreSQL and includes two relational tables: users and tasks. Useful for educational purposes, database modeling practice, or small-scale applications.
